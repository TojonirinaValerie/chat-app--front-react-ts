enum NavigationRoute {
  LOGIN = "/signin",
  REGISTER = "/signup",
  UPLOAD_PROFIL = "/upload-profil",
  DASHBOARD = "/dashboard",
  CHAT = "/chat",
  RELATION = "/relation",
  HELP = "/help-center",
  SETTING = "/setting",
  REPORT = "/report",
  FRINEDS = "/friends",
  REQUEST = "/request",
  MY_REQUEST = "/my-request",
  MY_REJECTED_REQUEST = "/my-rejected-request",
  SUGGESTIONS = "/suggestions",
}

export default NavigationRoute;
